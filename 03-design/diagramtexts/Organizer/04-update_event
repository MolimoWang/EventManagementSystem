actor "Organizer" as a

control "<link:_>: EventSystem" as ec
entity "<link:_>event : Event" as e
participant "<link:_>: EventService" as es

entity "<link:_>venue : Venue" as v
participant "<link:_>: AccountService" as as
entity "<link:_>ta : TicketAvailability" as ta
entity "<link:_>tq : TicketQuantity" as tq




==1. The organizer selects the update event functionality==

a->ec: showUpdateEvent(eventName)
activate ec

ec->es:event := getEvent(eventName)

ec->ec:displayUpdateEventPage(event)

ec-->a:
deactivate ec

==2. The system displays the add event page (18-modify-event) page\n3. The manager updates the details of the event,including: \n    -EventName\n    -EventDescription\n    -Date\n    -Time\n    -Venue\n    -TicketTypes\n    -TicketPrices==

a->ec:updateEvent(details)
activate ec

ec->es:updateEvent(eventName, newEventDescription, eventDate,newEventTime, \n newVenue, newTicketTypes : String[*], newTicketPrices : double[*])
activate es
es->es: event := getEvent(eventName)
es->e:venue := getVenue()



es->v:ta := getTicketAvailabilities()
loop for all ticket types
  es->ta:getTicketTypeName()
  es->ta:getNumberAvailable()
  es->tq:TicketQuantity(newTypeName, numAvailable, newPrice)
end

es->e:setDescription(newEventDescription)
es->e:setTime(newEventTime)
es->e:setDate(newEventDate)
es->e:setTicketQuantities(newTicketQuantities)

es->es:saveEvent(event)

es-->ec: return Error.NONE
deactivate es

ec->es:event := getEvent(eventName)

ec->ec:displayViewEventPage(event)

ec-->a:
deactivate ec

==4. The system shows the details of the updated event on the view event page (16-view-event)==