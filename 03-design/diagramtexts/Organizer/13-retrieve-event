title Retrieve

actor "Admin" as a
control ": EventSystem" as s
entity ": event:Event " as e
participant ": EventService" as u
entity ": venue:Venue " as v
participant ": AccountService" as c
entity ": TicketAvailability " as ta
entity ": TicketQuantity " as tq

note right of a: 1. The administrator wants to\n retrieve details of a specific event
a->s:viewEvent(eventName, eventDate)
activate s


note right of s: 2. The system passes requests to the service.
s->u: getEvents()

activate u

deactivate u

note right of s: 3. The system displays the page of event details.
s->s:displayEventDetailsPage(eventDetails)

s-->a:
deactivate s
