title Organizer View Venue Bookings Process

actor Organizer


boundary EventSystem
control EventService
entity Venue
activate EventSystem

note left of EventSystem:1. The organizer selects one specific date and time, \nthen selects the "Event Venue" option \n from the create event page (19-create_event.png).
Organizer ->> EventSystem : getAvailableVenues(date:Date, time:Time)
EventSystem->>EventService: getAvailableVenues(date:Date, time:Time)

activate Venue





loop For every Venues
 EventService->>Venue: getVenue(name)
  Venue-->EventService:return name
 EventService->>Venue: getVenueAddress(name)
  Venue-->EventService:return address
 end
  Venue-->EventService:return corresponding venue details list
 EventService-->EventSystem:return corresponding venue details list



 deactivate Venue


deactivate EventService

note over EventSystem:2. The system displays list of all the venues available for booking \n for the time period which the organizer chosen in this page.

EventSystem->EventSystem:return corresponding Venue Details list
EventSystem-->Organizer:
deactivate EventSystem

note over EventSystem:3. Organizer select one available venue from the list \n to be the venue which the events will use.
