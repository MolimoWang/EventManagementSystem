title Organizer Search Filter Events


actor "Organizer" as a
control ": EventSystem" as s
participant ": EventService" as u
entity "all : Event" as m
entity "filterEvents : Event" as sm
== 1. The organizer is on their main page **(17-view_all_events.png)**.==

note right of a:The organizer inputs the name of the event they want to search for in the filter box.
note right of a:The organizer click the 'Search' button.

a->s:filterEvents(keyWords)
activate s

s->u: getEvents(keyWords)
activate u

loop [for every Event]
u -> m: getDate()
end loop
u-->s:return filterEvents
deactivate u

s->s:displayEvents(filterEvents)

s-->a:
deactivate s

==The system displays the search results.==