title Actor Create Venue Details

actor Actor
boundary VenueSystem
control VenueService
entity Venue
entity TicketType


note right of Actor:1. The organiser or administrator selects\nthe add venue functionality.
Actor ->> VenueSystem : createVenue(venueDetails: VenueDetails)

activate VenueSystem
note right of VenueSystem:2. The system turn to create venue page(26-create_venue.png)\nwhich displays a form to enter venue details,including:\nName\nAddress\nTicket Types(ticketTypeName, number)\nContact information (name, phone, email)

    VenueSystem ->> VenueService : createVenue(venueDetails: VenueDetails)
    activate VenueService



 VenueService ->> Venue : Venue(name, address, contactName, phoneNumber, contactEmail, ticketType)
activate Venue
        Venue->>TicketType:TicketType(ticketTypeName,number)
TicketType -->Venue:return ticketType

Venue --> VenueService : return v

deactivate Venue

note right of VenueService:3. The organizer fills in the required details and clicks "Submit".

    VenueService ->> VenueService : checkDuplicateVenue(v)
    activate VenueService
    VenueService --> VenueService : return isDuplicate
    deactivate VenueService

    alt isDuplicate == false


        VenueService ->> VenueService : saveVenue(v)
        activate VenueService
        VenueService --> VenueService :
        deactivate VenueService

        VenueService -->> VenueSystem : return true
    else isDuplicate == true
        VenueService -->> VenueSystem : return false
deactivate VenueSystem
deactivate VenueService
end
note right of Actor: 4. The system updates the list of upcoming events\nand return to the list venues page (25-list_my_venues.png).
VenueSystem -->> Actor :
